# Omni-RL-Isaac

## Create Diretory mlp_policy
When you are training your agent, you need a directory in the package that will store the generated policies. This directory is called "mlp_policy". To create the directory, input the following in terminal:

touch mlp_policy
